# TYPE  DATABASE    USER        CIDR-ADDRESS          METHOD
local   all         postgres                          ident
local   all         all                               trust
host    all         all         127.0.0.1/32          trust

# allow replication connections from replication pg instance.
host    replication all         173.255.236.70/32     md5
